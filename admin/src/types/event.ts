export interface EventType {
  id: string;
  name: string;
  location: string;
  date: string;
  description?: string;
}

export interface formType {
  name: string;
  location: string;
  date: string;
  description: string;
}
